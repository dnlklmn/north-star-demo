"""
Evaluates whether output is a single commit message only, with no rationale, educational commentary, code explanation, or documentation.

Generated by North Star.
session_id: f1713d38-51a9-4696-914f-7978d00c1f9a
scope: skill__f1713d38
type: alignment
generated_at: 2026-05-07T11:00:11+00:00
"""

def alignment_scope_boundary(output: str, input: str) -> float:
    """
    Scores whether output stays within scope: a single commit message only,
    with no rationale, educational commentary, code explanation, or documentation.
    """
    judge_prompt = f"""You are evaluating whether a commit message output respects scope boundaries.

The output should:
- Be ONLY a single commit message
- NOT include explanations of why the change was made
- NOT include what the code does or how it works
- NOT include feedback on the user's proposed message
- NOT include educational commentary or rationale
- NOT include documentation or examples
- NOT include commit body or multi-line content

Output to evaluate:
{output}

Good examples (in-scope, message only):
- "feat(auth): add token refresh mechanism"
- "fix(parser): handle empty input gracefully"

Bad examples (out-of-scope, includes extra content):
- "feat(auth): add token refresh mechanism\n\nThis change improves security by allowing tokens to be refreshed without re-authentication."
- "fix(parser): handle empty input gracefully\n\nWhy: The parser was crashing when given empty strings. This fix validates input before processing."
- "Your message 'add auth tokens' should be formatted as: feat(auth): add token refresh mechanism. This follows conventional commits."
- "feat(auth): add token refresh mechanism\n\nExplanation: This allows users to maintain sessions longer without logging in again."

Score this output on a scale of 0.0 to 1.0 where:
- 1.0 = only the commit message, no extra content
- 0.5 = mostly just the message but with minor extra commentary
- 0.0 = includes significant rationale, explanation, feedback, or multi-line content

Respond with only a single float between 0.0 and 1.0."""
    return call_judge(judge_prompt)

