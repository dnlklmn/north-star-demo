"""
Evaluates whether output properly handles structured change context (type, scope, description) and generates a formatted commit message.

Generated by North Star.
session_id: f1713d38-51a9-4696-914f-7978d00c1f9a
scope: skill__f1713d38
type: coverage
generated_at: 2026-05-07T11:00:11+00:00
"""

def coverage_structured_context_input(output: str, input: str) -> float:
    """
    Scores whether the output properly handles structured change context
    (type, scope, description) and generates a formatted commit message.
    """
    judge_prompt = f"""You are evaluating whether a commit message generator properly handles structured input.

The input provided to the system:
{input}

The output generated:
{output}

Evaluate whether:
1. The output is a valid conventional commit message (type(scope): description)
2. The message uses the type provided in the input (or infers correctly if not provided)
3. The message uses the scope provided in the input (or infers correctly if not provided)
4. The message incorporates the description provided in the input
5. The message fits within 72 characters
6. The message is properly formatted (lowercase, no trailing punctuation)

Good scenario:
- Input: "Type: feat, Scope: auth, Description: add token refresh mechanism"
- Output: "feat(auth): add token refresh mechanism"

Bad scenario:
- Input: "Type: fix, Scope: parser, Description: handle empty input"
- Output: "feat(parser): add new parsing capabilities" (wrong type, ignores description)

Score this output on a scale of 0.0 to 1.0 where:
- 1.0 = properly formats structured input into conventional commits
- 0.5 = mostly correct but may have minor formatting or interpretation issues
- 0.0 = fails to use provided context or produces invalid format

Respond with only a single float between 0.0 and 1.0."""
    return call_judge(judge_prompt)

