"""
Evaluates whether output is deterministic (same input produces same output) with no alternatives, options, or variations offered.

Generated by North Star.
session_id: f1713d38-51a9-4696-914f-7978d00c1f9a
scope: skill__f1713d38
type: alignment
generated_at: 2026-05-07T11:00:11+00:00
"""

def alignment_determinism(output: str, input: str) -> float:
    """
    Scores whether the output is deterministic with no alternatives,
    options, or variations offered to the user.
    """
    judge_prompt = f"""You are evaluating whether a commit message output is deterministic.

The output should:
- Be a single, definitive commit message
- NOT include multiple suggestions or alternatives
- NOT use words like "or", "alternatively", "you could also", "option 1", "option 2"
- NOT ask the user to choose between options
- NOT hedge or suggest variations

Output to evaluate:
{output}

Good examples (deterministic, single message):
- "feat(auth): add token refresh mechanism"
- "fix(parser): handle empty input gracefully"

Bad examples (non-deterministic, offers alternatives):
- "feat(auth): add token refresh mechanism or implement session renewal"
- "You could write: 'feat(auth): add tokens' or 'feat(auth): implement refresh'"
- "fix(parser): handle empty input (or you could validate on entry)"
- "Option 1: feat(auth): add tokens\nOption 2: feat(auth): refresh session"

Score this output on a scale of 0.0 to 1.0 where:
- 1.0 = single, definitive message with no alternatives or hedging
- 0.5 = mostly deterministic but contains minor hedging language
- 0.0 = multiple suggestions, alternatives, or asks user to choose

Respond with only a single float between 0.0 and 1.0."""
    return call_judge(judge_prompt)

