"""
Evaluates whether output follows conventional commits structure with correct type, optional scope, description, no trailing punctuation, and total length ≤72 characters.

Generated by North Star.
session_id: f1713d38-51a9-4696-914f-7978d00c1f9a
scope: skill__f1713d38
type: alignment
generated_at: 2026-05-07T11:00:11+00:00
"""

def alignment_message_format(output: str, input: str) -> float:
    """
    Scores whether the output follows the conventional commits format:
    type(scope): description with no trailing punctuation and ≤72 chars.
    """
    judge_prompt = f"""You are evaluating a commit message generator's output.

The output should strictly follow the conventional commits format:
- Structure: type(scope): description
- Valid types: feat, fix, docs, style, refactor, perf, test, chore
- Scope is optional but recommended
- Description must be lowercase and use imperative mood
- No trailing punctuation (no period, exclamation mark, etc.)
- Total length must be ≤72 characters
- No multiple lines

Output to evaluate:
{output}

Good examples:
- "feat(auth): add token refresh mechanism"
- "fix: prevent null pointer in parser"
- "refactor(api): simplify endpoint routing for maintainability"

Bad examples:
- "feat(auth): Add token refresh mechanism." (capitalized, trailing period)
- "feat(auth): add token refresh mechanism that improves security and performance" (exceeds 72 chars)
- "feat(auth): add token refresh mechanism\nAlso refactored the auth module" (multiple lines)

Score this output on a scale of 0.0 to 1.0 where:
- 1.0 = perfectly follows format (correct type, optional scope, lowercase description, no punctuation, ≤72 chars, single line)
- 0.5 = minor format violations (e.g., slightly over 72 chars, or one formatting issue)
- 0.0 = major format violations (wrong structure, multiple lines, trailing punctuation, significantly over limit)

Respond with only a single float between 0.0 and 1.0."""
    return call_judge(judge_prompt)

