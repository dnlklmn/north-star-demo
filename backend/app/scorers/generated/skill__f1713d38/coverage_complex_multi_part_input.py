"""
Evaluates whether output properly handles complex multi-part changes and provides guidance on breaking them into separate commits.

Generated by North Star.
session_id: f1713d38-51a9-4696-914f-7978d00c1f9a
scope: skill__f1713d38
type: coverage
generated_at: 2026-05-07T11:00:11+00:00
"""

def coverage_complex_multi_part_input(output: str, input: str) -> float:
    """
    Scores whether the output properly handles complex multi-part changes
    and provides guidance on breaking them into separate commits.
    """
    judge_prompt = f"""You are evaluating whether a commit message generator properly handles complex multi-part changes.

The input provided to the system:
{input}

The output generated:
{output}

For complex multi-part changes, the system should:
1. Recognize that the change spans multiple concerns
2. Either:
   a) Generate a single message for the primary concern, OR
   b) Suggest breaking into multiple commits with guidance on how to split them
3. If suggesting multiple commits, provide clear, actionable guidance
4. Each suggested commit should follow conventional commits format

Good scenario:
- Input: "Refactored the auth module AND added token refresh AND updated tests"
- Output: "Consider breaking into:\n1. refactor(auth): simplify token handling\n2. feat(auth): add token refresh\n3. test(auth): update token tests"
  OR
- Output: "feat(auth): add token refresh mechanism" (if focusing on primary change)

Bad scenario:
- Input: "Refactored auth AND added tokens AND updated tests"
- Output: "feat(auth): refactor and add tokens and update tests" (tries to cram everything into one message)

Score this output on a scale of 0.0 to 1.0 where:
- 1.0 = recognizes complexity and either picks primary concern or provides clear multi-commit guidance
- 0.5 = handles complexity but guidance could be clearer or more actionable
- 0.0 = tries to cram multiple concerns into one message or fails to recognize complexity

Respond with only a single float between 0.0 and 1.0."""
    return call_judge(judge_prompt)

