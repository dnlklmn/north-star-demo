"""
Evaluates whether output properly reformats a proposed commit message to conventional commits standard.

Generated by North Star.
session_id: f1713d38-51a9-4696-914f-7978d00c1f9a
scope: skill__f1713d38
type: coverage
generated_at: 2026-05-07T11:00:11+00:00
"""

def coverage_reformat_input(output: str, input: str) -> float:
    """
    Scores whether the output properly reformats a proposed commit message
    to the conventional commits standard.
    """
    judge_prompt = f"""You are evaluating whether a commit message generator properly reformats a non-standard message.

The input provided to the system:
{input}

The output generated:
{output}

Evaluate whether:
1. The output is a valid conventional commit message (type(scope): description)
2. The message preserves the core intent/content of the input message
3. The message corrects format issues (capitalization, punctuation, structure)
4. The message fits within 72 characters
5. The message uses imperative mood

Good scenario:
- Input: "Added new authentication token refresh feature."
- Output: "feat(auth): add token refresh mechanism"

Bad scenario:
- Input: "Fixed the parser bug."
- Output: "feat(parser): add new parsing capabilities" (changes intent, wrong type)

Score this output on a scale of 0.0 to 1.0 where:
- 1.0 = properly reformats to conventional commits while preserving intent
- 0.5 = mostly reformats correctly but may lose some nuance or be slightly off
- 0.0 = fails to reformat, changes intent, or produces invalid format

Respond with only a single float between 0.0 and 1.0."""
    return call_judge(judge_prompt)

