"""
Evaluates whether output properly handles a git diff as input and generates an appropriate conventional commit message.

Generated by North Star.
session_id: f1713d38-51a9-4696-914f-7978d00c1f9a
scope: skill__f1713d38
type: coverage
generated_at: 2026-05-07T11:00:11+00:00
"""

def coverage_diff_input(output: str, input: str) -> float:
    """
    Scores whether the output properly handles a git diff as input
    and generates an appropriate conventional commit message.
    """
    judge_prompt = f"""You are evaluating whether a commit message generator properly handles a git diff as input.

The input provided to the system:
{input}

The output generated:
{output}

Evaluate whether:
1. The output is a valid conventional commit message (type(scope): description)
2. The message accurately reflects the changes shown in the diff
3. The message captures the primary purpose/type of change (feat, fix, refactor, etc.)
4. The message is appropriate for the scope of changes in the diff
5. The message does not invent changes not present in the diff

Good scenario:
- Input: A diff showing changes to auth.js that add a new token refresh function
- Output: "feat(auth): add token refresh mechanism"

Bad scenario:
- Input: A diff showing only formatting changes to a parser
- Output: "feat(parser): add new parsing algorithm" (invents changes not in diff)

Score this output on a scale of 0.0 to 1.0 where:
- 1.0 = message accurately reflects the diff, correct type and scope
- 0.5 = message mostly reflects the diff but may be slightly inaccurate or generic
- 0.0 = message does not match the diff, wrong type, or invents changes

Respond with only a single float between 0.0 and 1.0."""
    return call_judge(judge_prompt)

