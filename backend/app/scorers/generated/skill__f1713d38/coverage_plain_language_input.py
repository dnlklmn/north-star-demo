"""
Evaluates whether output properly handles plain-language change descriptions and generates an appropriate conventional commit message.

Generated by North Star.
session_id: f1713d38-51a9-4696-914f-7978d00c1f9a
scope: skill__f1713d38
type: coverage
generated_at: 2026-05-07T11:00:11+00:00
"""

def coverage_plain_language_input(output: str, input: str) -> float:
    """
    Scores whether the output properly handles plain-language change descriptions
    and generates an appropriate conventional commit message.
    """
    judge_prompt = f"""You are evaluating whether a commit message generator properly handles plain-language input.

The input provided to the system:
{input}

The output generated:
{output}

Evaluate whether:
1. The output is a valid conventional commit message (type(scope): description)
2. The message accurately captures the intent described in plain language
3. The message infers an appropriate type (feat, fix, refactor, etc.) from the description
4. The message infers an appropriate scope if the description mentions a component
5. The message does not invent details not mentioned in the input

Good scenario:
- Input: "We need to add a new authentication token refresh feature to prevent session timeouts"
- Output: "feat(auth): add token refresh to prevent session timeouts"

Bad scenario:
- Input: "Fix the bug where the parser crashes on empty input"
- Output: "feat(parser): add new parsing capabilities" (wrong type, doesn't address the bug)

Score this output on a scale of 0.0 to 1.0 where:
- 1.0 = message accurately captures the plain-language intent, correct type and scope
- 0.5 = message mostly captures intent but may be slightly off or generic
- 0.0 = message does not match the description, wrong type, or invents details

Respond with only a single float between 0.0 and 1.0."""
    return call_judge(judge_prompt)

