"""
Evaluates whether output captures what changed and why in a single terse line without filler, redundancy, or verbose phrasing.

Generated by North Star.
session_id: f1713d38-51a9-4696-914f-7978d00c1f9a
scope: skill__f1713d38
type: alignment
generated_at: 2026-05-07T11:00:11+00:00
"""

def alignment_conciseness(output: str, input: str) -> float:
    """
    Scores whether the output is concise, capturing both what changed and why
    without filler, redundancy, or verbose phrasing.
    """
    judge_prompt = f"""You are evaluating a commit message for conciseness.

The message should:
- Capture both WHAT changed and WHY it matters in a single line
- Avoid filler words, redundant phrasing, or over-explanation
- Use imperative mood ("add" not "added")
- Omit unnecessary articles ("the", "a")
- Be terse but not cryptic

Output to evaluate:
{output}

Good examples (concise, captures what and why):
- "fix(auth): reject expired tokens to prevent session hijacking"
- "refactor(parser): simplify token matching for maintainability"
- "perf(db): cache query results to reduce latency"

Bad examples (verbose, redundant, or unclear):
- "feat(auth): we have added a new feature that allows users to refresh their authentication tokens" (verbose, uses "we have added")
- "fix: fixed the bug" (redundant, doesn't explain what or why)
- "refactor(parser): refactored the parser module to make it better" (vague, doesn't explain the benefit)

Score this output on a scale of 0.0 to 1.0 where:
- 1.0 = terse and clear, captures what and why without filler
- 0.5 = mostly concise but has some redundancy or could be tighter
- 0.0 = verbose, redundant, or fails to explain what/why

Respond with only a single float between 0.0 and 1.0."""
    return call_judge(judge_prompt)

