# Environment variables
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
.eggs/
*.egg-info/
*.egg

# Node
node_modules/
dist/
.npm
*.tsbuildinfo

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Database
*.db
*.sqlite

# Testing
coverage/
.coverage
htmlcov/
.pytest_cache/

# Build
build/
*.manifest

# Eval agent-mode sandboxes (per-row scratch dirs created by eval_runner)
backend/tmp/
tmp/eval-runs/

# Agent worktrees and stray local checkouts — must never be tracked as
# gitlinks (a previous accident committed `collaborative` and
# `.claude/worktrees/angry-shirley` as submodule-like entries, which broke
# `git status` repo-wide).
.claude/worktrees/
/collaborative/

# Local-only working content (private business docs; MCP server scratch DBs)
# — kept on disk but never pushed to the (north-star-demo) remote.
/business/
.mcp/
